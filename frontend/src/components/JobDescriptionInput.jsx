import { useState } from 'react'; import { ArrowRight, BriefcaseBusiness } from 'lucide-react';
export default function JobDescriptionInput({ job, busy, onAnalyze }) {
  const [title, setTitle] = useState(job?.title || ''); const [company, setCompany] = useState(job?.company || ''); const [description, setDescription] = useState(job?.description || '');
  return <div className="panel p-5"><div className="mb-5 flex items-center gap-2"><BriefcaseBusiness size={17} className="text-aqua"/><h2 className="text-sm font-semibold">Target role</h2></div><div className="grid gap-3 sm:grid-cols-2"><label className="text-xs text-slate-400">Role title<input className="input mt-1" value={title} onChange={e => setTitle(e.target.value)} placeholder="Senior Product Designer"/></label><label className="text-xs text-slate-400">Company <span className="text-slate-600">optional</span><input className="input mt-1" value={company} onChange={e => setCompany(e.target.value)} placeholder="Acme"/></label></div><label className="mt-3 block text-xs text-slate-400">Job description<textarea className="input mt-1 min-h-32 resize-y" value={description} onChange={e => setDescription(e.target.value)} placeholder="Paste the full job description…"/></label><button type="button" disabled={busy || description.length < 80 || title.length < 2} onClick={() => onAnalyze({ title, company, description })} className="button-primary mt-4 w-full disabled:cursor-not-allowed disabled:opacity-40">{busy ? 'Reading role…' : <>Analyse fit <ArrowRight size={15}/></>}</button></div>;
}
