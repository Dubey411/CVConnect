import { useRef, useState } from 'react'; import { FileUp, FileText, Sparkles } from 'lucide-react'; import { motion } from 'framer-motion';
export default function ResumeUpload({ resume, busy, onUpload }) {
  const input = useRef(); const [dragging, setDragging] = useState(false); const accept = (file) => file && onUpload(file);
  if (resume) return <div className="panel flex items-center gap-3 p-4"><div className="grid h-10 w-10 place-items-center bg-aqua text-ink"><FileText size={19}/></div><div className="min-w-0 flex-1"><p className="text-sm font-semibold">{resume.original?.contact?.name || 'Resume parsed'}</p><p className="truncate font-mono text-[10px] text-slate-400">{resume.original?.contact?.email || 'Structured resume ready'} · {Math.round((resume.original?.confidence || 0) * 100)}% parsing confidence</p></div><Sparkles size={16} className="text-aqua"/></div>;
  return <motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} onDragOver={e => { e.preventDefault(); setDragging(true); }} onDragLeave={() => setDragging(false)} onDrop={e => { e.preventDefault(); setDragging(false); accept(e.dataTransfer.files[0]); }} className={`group border border-dashed p-7 text-center transition ${dragging ? 'border-aqua bg-aqua/10' : 'border-line bg-surface/30 hover:border-aqua/60'}`}><input ref={input} className="sr-only" type="file" accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" onChange={e => accept(e.target.files[0])}/><div className="mx-auto mb-4 grid h-12 w-12 place-items-center border border-line bg-ink group-hover:border-aqua"><FileUp className="text-aqua" size={21}/></div><p className="text-sm font-semibold">Drop your resume here</p><p className="mt-1 text-xs text-slate-400">PDF or DOCX · 10 MB maximum</p><button type="button" disabled={busy} onClick={() => input.current.click()} className="button-primary mt-5 disabled:opacity-50">{busy ? 'Parsing…' : 'Choose file'}</button></motion.div>;
}
